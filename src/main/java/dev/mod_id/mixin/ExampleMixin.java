package dev.mod_id.mixin;

import dev.mod_id.config.ModConfig;
import dev.mod_id.config.ModConfigData;

@Mixin(PackageName.class)
public class ExampleMixin {

  // to access config variable value use:
  // ModConfig.CONFIG.variable_name
  
}
